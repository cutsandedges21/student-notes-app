node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log

# Bulk Google Fonts download; the app loads web fonts from the CDN instead.
# 151MB of unreferenced binaries has no business in git history.
fonts/

# Playwright MCP scratch output and Supabase CLI temp state.
.playwright-mcp/
supabase/.temp/

# Playwright output.
test-results/
playwright-report/

# Generated single-file SQL bundle; migrations are the source of truth.
supabase/.generated/

# Vercel CLI link metadata and any env files it pulls down. The example is
# tracked deliberately: it is the list of names, never the values.
.vercel
.env*
!.env.example
